package com.nuvio.tv.data.repository

// Emptied: dead code, the implementation of the now-emptied
// domain/repository/SyncRepository.kt (see that file for why). Left as an
// empty file rather than deleted for this session, per environment
// restrictions on file deletion; safe to delete outright in a normal editor
// (along with the SyncRepository interface and its RepositoryModule.kt
// binding).
