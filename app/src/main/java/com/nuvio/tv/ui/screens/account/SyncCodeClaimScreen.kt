package com.nuvio.tv.ui.screens.account

// Emptied: dead code (SyncCodeClaimScreen() had no callers and
// Screen.SyncCodeClaim had no composable registration in NuvioNavHost.kt -
// the manual sync-code pairing flow was superseded by QR sign-in). Left as an
// empty file rather than deleted for this session, per environment
// restrictions on file deletion; safe to delete outright in a normal editor.
